import { Lesson } from "@/types";

export const cLessons: Lesson[] = [
  { id: 1, title: "Introduction to C", duration: "12 min", content: `<p>C is a powerful, low-level programming language created by Dennis Ritchie at Bell Labs in 1972. It's the foundation of modern computing — the Linux kernel, Python interpreter, and many operating systems are written in C.</p><p>C compiles to machine code, giving it unmatched performance and direct memory access.</p><h4>Why Learn C?</h4><ul><li>Understand how computers really work</li><li>Foundation for C++, Java, Python internals</li><li>Embedded systems, OS development</li><li>Maximum performance</li></ul>`, codeExample: `#include <stdio.h>\n\nint main() {\n    printf("Hello, Alien Code!\\n");\n    \n    // C is expressive and fast\n    int x = 42;\n    float pi = 3.14159f;\n    printf("x = %d, pi = %.2f\\n", x, pi);\n    \n    return 0;  // 0 means success\n}`, quiz: { question: "Who created the C programming language?", options: ["Guido van Rossum", "Dennis Ritchie", "James Gosling", "Bjarne Stroustrup"], correct: 1, explanation: "Dennis Ritchie created C at Bell Labs in 1972. It remains one of the most influential languages ever created." } },
  { id: 2, title: "Compilation & Structure", duration: "11 min", content: `<p>C is a <strong>compiled</strong> language — source code is transformed to machine code by a compiler (like GCC or Clang) before running.</p><h4>Compilation steps:</h4><ol><li>Preprocessing — handles #include, #define</li><li>Compilation — converts to assembly</li><li>Assembly — converts to object code</li><li>Linking — combines with libraries</li></ol>`, codeExample: `// file: hello.c\n#include <stdio.h>   // Standard I/O header\n#include <stdlib.h>  // Standard library\n\n// main() is the entry point of every C program\nint main(int argc, char *argv[]) {\n    printf("Args: %d\\n", argc);\n    \n    if (argc > 1) {\n        printf("First arg: %s\\n", argv[1]);\n    }\n    \n    return EXIT_SUCCESS;  // = 0\n}\n\n// Compile: gcc -Wall -o hello hello.c\n// Run: ./hello myarg`, quiz: { question: "What is the entry point function in a C program?", options: ["start()", "run()", "main()", "begin()"], correct: 2, explanation: "Every C program must have a main() function — this is where execution begins." } },
  { id: 3, title: "Variables & Data Types", duration: "13 min", content: `<p>C is <strong>statically typed</strong> — you must declare the type of every variable. C's types map directly to hardware memory representations.</p><h4>Fundamental Types:</h4><ul><li><code>char</code> — 1 byte (-128 to 127 or a character)</li><li><code>int</code> — 4 bytes (typically) — whole numbers</li><li><code>float</code> — 4 bytes — decimal numbers</li><li><code>double</code> — 8 bytes — more precise decimals</li><li><code>long</code> — at least 4 bytes, often 8</li></ul>`, codeExample: `#include <stdio.h>\n#include <limits.h>  // INT_MAX, INT_MIN, etc.\n\nint main() {\n    char c = 'A';\n    int age = 25;\n    float score = 98.5f;\n    double pi = 3.14159265358979;\n    long bigNum = 9876543210L;\n    unsigned int positive = 42u;\n    \n    printf("char: %c (%d)\\n", c, c);\n    printf("int: %d\\n", age);\n    printf("float: %.1f\\n", score);\n    printf("double: %.10f\\n", pi);\n    printf("Max int: %d\\n", INT_MAX);\n    return 0;\n}`, quiz: { question: "How many bytes does a float typically occupy?", options: ["1 byte", "2 bytes", "4 bytes", "8 bytes"], correct: 2, explanation: "A float typically occupies 4 bytes (32 bits) providing about 7 decimal digits of precision." } },
  { id: 4, title: "printf & scanf", duration: "12 min", content: `<p><code>printf</code> outputs formatted text. <code>scanf</code> reads formatted input. Both use format specifiers.</p><h4>Format Specifiers:</h4><ul><li><code>%d</code> — int, <code>%f</code> — float, <code>%lf</code> — double</li><li><code>%c</code> — char, <code>%s</code> — string</li><li><code>%x</code> — hex, <code>%o</code> — octal</li><li><code>%p</code> — pointer address</li><li><code>%zu</code> — size_t</li></ul>`, codeExample: `#include <stdio.h>\n\nint main() {\n    int age;\n    char name[50];\n    float height;\n    \n    printf("Enter your name: ");\n    scanf("%s", name);  // reads until whitespace\n    \n    printf("Enter age and height: ");\n    scanf("%d %f", &age, &height);\n    \n    printf("Name: %s\\n", name);\n    printf("Age: %d, Height: %.2f m\\n", age, height);\n    \n    // Formatted width/precision\n    printf("|%-10s|%6.2f|\\n", name, height);\n    return 0;\n}`, quiz: { question: "Which format specifier reads/prints a double with scanf/printf?", options: ["%f", "%d", "%lf", "%g"], correct: 2, explanation: "For scanf, use %lf for double. For printf, both %f and %lf work for double (since C99 promotes float to double)." } },
  { id: 5, title: "Operators", duration: "11 min", content: `<p>C supports arithmetic, relational, logical, bitwise, and assignment operators. Bitwise operators make C uniquely powerful for low-level programming.</p><h4>Bitwise Operators:</h4><ul><li><code>&</code> — AND, <code>|</code> — OR, <code>^</code> — XOR</li><li><code>~</code> — NOT, <code>&lt;&lt;</code> — left shift, <code>&gt;&gt;</code> — right shift</li></ul>`, codeExample: `#include <stdio.h>\n\nint main() {\n    int a = 5, b = 3;\n    \n    printf("%d + %d = %d\\n", a, b, a + b);\n    printf("%d %% %d = %d\\n", a, b, a % b); // 2\n    printf("a++ = %d\\n", a++); // post: returns 5, then a=6\n    printf("++b = %d\\n", ++b); // pre: b=4, returns 4\n    \n    // Bitwise\n    printf("5 & 3 = %d\\n", 5 & 3);  // 001 = 1\n    printf("5 | 3 = %d\\n", 5 | 3);  // 111 = 7\n    printf("5 ^ 3 = %d\\n", 5 ^ 3);  // 110 = 6\n    printf("1 << 3 = %d\\n", 1 << 3); // 8\n    return 0;\n}`, quiz: { question: "What does the left shift operator << do?", options: ["Moves bits to the left, multiplying by 2 per shift", "Moves string characters left", "Integer division by 2", "Reverses the bits"], correct: 0, explanation: "Left shift (<<) moves bits to the left. Each shift left multiplies by 2. 1 << 3 = 8 (1 × 2³)." } },
  { id: 6, title: "Conditionals", duration: "11 min", content: `<p>C uses if/else, switch/case, and the ternary operator for conditional logic. The switch statement is particularly efficient for multiple conditions.</p>`, codeExample: `#include <stdio.h>\n\nint main() {\n    int score = 85;\n    char grade;\n    \n    // Ternary operator\n    grade = (score >= 90) ? 'A' : (score >= 80) ? 'B' : 'C';\n    printf("Grade: %c\\n", grade);\n    \n    // Switch statement\n    switch (grade) {\n        case 'A':\n            printf("Excellent!\\n");\n            break;\n        case 'B':\n            printf("Good!\\n");\n            break;\n        default:\n            printf("Keep going!\\n");\n    }\n    return 0;\n}`, quiz: { question: "What happens in a switch statement without a break?", options: ["Exits the switch", "Skips the case", "Falls through to the next case", "Causes a compile error"], correct: 2, explanation: "Without break, execution 'falls through' to the next case statement, which is sometimes intentional but often a bug." } },
  { id: 7, title: "Loops", duration: "13 min", content: `<p>C provides three loop types: <code>for</code>, <code>while</code>, and <code>do-while</code>. The do-while executes at least once, unlike while and for.</p>`, codeExample: `#include <stdio.h>\n\nint main() {\n    // For loop\n    for (int i = 0; i < 5; i++) {\n        printf("%d ", i);\n    }\n    printf("\\n");\n    \n    // While loop\n    int n = 1;\n    while (n <= 8) {\n        printf("%d ", n);\n        n *= 2;\n    }\n    printf("\\n");\n    \n    // Do-while (runs at least once)\n    int x;\n    do {\n        printf("Enter positive number: ");\n        scanf("%d", &x);\n    } while (x <= 0);\n    \n    printf("You entered: %d\\n", x);\n    return 0;\n}`, quiz: { question: "What makes a do-while loop unique?", options: ["It runs in reverse", "It always executes the body at least once", "It can't use break", "It requires an iterator"], correct: 1, explanation: "The do-while loop checks its condition after executing the body, guaranteeing at least one execution." } },
  { id: 8, title: "Functions", duration: "14 min", content: `<p>C functions must be declared before use (or have a forward declaration/prototype). C uses <strong>pass by value</strong> — functions get copies of arguments.</p>`, codeExample: `#include <stdio.h>\n\n// Function prototype (forward declaration)\nint factorial(int n);\nvoid swap(int *a, int *b);  // using pointers for by-reference\n\nint main() {\n    printf("5! = %d\\n", factorial(5));\n    \n    int x = 10, y = 20;\n    printf("Before: %d %d\\n", x, y);\n    swap(&x, &y);\n    printf("After: %d %d\\n", x, y);\n    return 0;\n}\n\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nvoid swap(int *a, int *b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}`, quiz: { question: "What is a function prototype in C?", options: ["A template for creating functions", "A declaration of function signature before its definition", "A built-in function", "An inline function"], correct: 1, explanation: "A function prototype declares the function's return type, name, and parameter types before the actual definition, allowing calls before definition." } },
  { id: 9, title: "Arrays", duration: "14 min", content: `<p>C arrays are fixed-size, contiguous blocks of memory. Array indexing starts at 0. The array name is a pointer to the first element.</p><p>C does NOT check array bounds — accessing out-of-bounds causes undefined behavior!</p>`, codeExample: `#include <stdio.h>\n\nint main() {\n    // Declaration and initialization\n    int nums[5] = {10, 20, 30, 40, 50};\n    float grades[] = {92.5f, 87.0f, 95.0f}; // size inferred\n    \n    // Access and modify\n    nums[2] = 99;\n    \n    // Loop through array\n    int sum = 0;\n    for (int i = 0; i < 5; i++) {\n        sum += nums[i];\n        printf("nums[%d] = %d\\n", i, nums[i]);\n    }\n    printf("Sum: %d, Avg: %.1f\\n", sum, (float)sum/5);\n    \n    // 2D array\n    int matrix[3][3] = {{1,2,3},{4,5,6},{7,8,9}};\n    printf("Center: %d\\n", matrix[1][1]); // 5\n    return 0;\n}`, quiz: { question: "What happens in C if you access an array out of bounds?", options: ["Index is clamped to valid range", "An exception is thrown", "Undefined behavior (crash or data corruption)", "Returns 0"], correct: 2, explanation: "C does not check array bounds. Out-of-bounds access causes undefined behavior — the program may crash, return garbage, or silently corrupt memory." } },
  { id: 10, title: "Strings in C", duration: "13 min", content: `<p>C strings are null-terminated character arrays — a sequence of chars ending with <code>'\\0'</code>. The <code>string.h</code> library provides functions.</p><h4>Key functions:</h4><ul><li><code>strlen()</code> — length</li><li><code>strcpy()</code> — copy (use strncpy for safety)</li><li><code>strcat()</code> — concatenate</li><li><code>strcmp()</code> — compare (returns 0 if equal)</li><li><code>sprintf()</code> — format into string</li></ul>`, codeExample: `#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char name[50] = "Alien";\n    char greeting[100];\n    \n    printf("Length: %zu\\n", strlen(name)); // 5\n    \n    // Concatenate safely\n    snprintf(greeting, sizeof(greeting), "Hello, %s!", name);\n    printf("%s\\n", greeting);\n    \n    // Compare\n    if (strcmp(name, "Alien") == 0) {\n        printf("Match!\\n");\n    }\n    \n    // Individual characters\n    for (int i = 0; name[i] != '\\0'; i++) {\n        printf("[%c]", name[i]);\n    }\n    return 0;\n}`, quiz: { question: "How are strings terminated in C?", options: ["With a space", "With a null character '\\0'", "With a newline '\\n'", "With a period"], correct: 1, explanation: "C strings are null-terminated — a '\\0' character marks the end of the string. strlen() counts characters up to this null terminator." } },
  { id: 11, title: "Pointers", duration: "18 min", content: `<p>Pointers store memory addresses. They are one of C's most powerful (and dangerous) features. The <code>&</code> operator gets an address, <code>*</code> dereferences (gets the value at the address).</p>`, codeExample: `#include <stdio.h>\n\nint main() {\n    int x = 42;\n    int *ptr = &x;  // ptr holds address of x\n    \n    printf("Value of x: %d\\n", x);\n    printf("Address of x: %p\\n", (void*)&x);\n    printf("Value of ptr: %p\\n", (void*)ptr);\n    printf("Dereference ptr: %d\\n", *ptr);\n    \n    *ptr = 100;  // Modify x through pointer\n    printf("x is now: %d\\n", x);\n    \n    // Pointer arithmetic\n    int arr[] = {10, 20, 30, 40};\n    int *p = arr;  // points to arr[0]\n    printf("%d %d %d\\n", *p, *(p+1), *(p+2));\n    return 0;\n}`, quiz: { question: "What does the dereference operator * do?", options: ["Gets the address of a variable", "Multiplies two values", "Accesses the value at a pointer's address", "Declares a constant"], correct: 2, explanation: "The * (dereference) operator accesses the value stored at the memory address a pointer holds." } },
  { id: 12, title: "Pointer Arithmetic", duration: "14 min", content: `<p>Pointer arithmetic lets you navigate memory. When you add 1 to a pointer, it advances by the size of the pointed-to type.</p><p>This is why arrays and pointers are closely related in C.</p>`, codeExample: `#include <stdio.h>\n\nvoid printArray(int *arr, int size) {\n    for (int *p = arr; p < arr + size; p++) {\n        printf("%d ", *p);\n    }\n    printf("\\n");\n}\n\nint main() {\n    int nums[] = {5, 10, 15, 20, 25};\n    int *p = nums;\n    \n    printf("First: %d\\n", *p);      // 5\n    printf("Third: %d\\n", *(p+2)); // 15\n    p++;  // advance to next int\n    printf("Now: %d\\n", *p);        // 10\n    \n    printArray(nums, 5);\n    \n    // Pointer difference\n    int *start = nums;\n    int *end = nums + 4;\n    printf("Distance: %td\\n", end - start); // 4\n    return 0;\n}`, quiz: { question: "If ptr points to int and you do ptr+1, how many bytes does the address advance?", options: ["1 byte", "2 bytes", "4 bytes (size of int)", "8 bytes"], correct: 2, explanation: "Pointer arithmetic advances by the size of the pointed-to type. For int (4 bytes), ptr+1 advances 4 bytes." } },
  { id: 13, title: "Dynamic Memory Allocation", duration: "16 min", content: `<p>C allows manual memory management via <code>malloc</code>, <code>calloc</code>, <code>realloc</code>, and <code>free</code>. This gives full control but requires discipline — forgetting to free memory causes <strong>memory leaks</strong>.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    // Allocate array of n ints at runtime\n    int n = 5;\n    int *arr = (int*)malloc(n * sizeof(int));\n    \n    if (arr == NULL) {\n        fprintf(stderr, "Memory allocation failed!\\n");\n        return 1;\n    }\n    \n    for (int i = 0; i < n; i++) arr[i] = i * 10;\n    for (int i = 0; i < n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n    \n    // Resize\n    arr = (int*)realloc(arr, 10 * sizeof(int));\n    for (int i = 5; i < 10; i++) arr[i] = i * 10;\n    \n    free(arr);  // Always free!\n    arr = NULL; // Prevent dangling pointer\n    return 0;\n}`, quiz: { question: "What must you always do after using malloc/calloc allocated memory?", options: ["Call reset()", "Call free() to release the memory", "Set it to NULL first", "Call close()"], correct: 1, explanation: "Always call free() on dynamically allocated memory when done. Failing to do so causes memory leaks." } },
  { id: 14, title: "Structures (struct)", duration: "15 min", content: `<p>Structures group related variables of different types into a single unit. They're the foundation of complex data organization in C.</p>`, codeExample: `#include <stdio.h>\n#include <string.h>\n\ntypedef struct {\n    char name[50];\n    int level;\n    float health;\n    int skills[5];\n} Player;\n\nvoid printPlayer(const Player *p) {\n    printf("Player: %s | Level: %d | HP: %.1f\\n",\n           p->name, p->level, p->health);\n}\n\nint main() {\n    Player p1 = {.name = "Alien", .level = 10, .health = 100.0f};\n    \n    // Modify\n    p1.level++;\n    strncpy(p1.name, "Alien Coder", 49);\n    \n    // Access via pointer\n    Player *ptr = &p1;\n    ptr->health -= 15.5f;\n    \n    printPlayer(&p1);\n    printf("Size: %zu bytes\\n", sizeof(Player));\n    return 0;\n}`, quiz: { question: "How do you access a struct member through a pointer?", options: ["ptr.member", "ptr[member]", "ptr->member", "*ptr.member"], correct: 2, explanation: "The arrow operator (->) accesses a struct member through a pointer. ptr->member is equivalent to (*ptr).member." } },
  { id: 15, title: "Enumerations & Unions", duration: "12 min", content: `<p><code>enum</code> creates named integer constants. <code>union</code> stores different types in the same memory location (only one at a time).</p>`, codeExample: `#include <stdio.h>\n\ntypedef enum {\n    SUNDAY = 0,\n    MONDAY,\n    TUESDAY,\n    WEDNESDAY,\n    THURSDAY,\n    FRIDAY,\n    SATURDAY\n} Day;\n\ntypedef union {\n    int i;\n    float f;\n    char str[16];\n} Data;\n\nint main() {\n    Day today = WEDNESDAY;\n    printf("Day: %d\\n", today);  // 3\n    if (today == WEDNESDAY) printf("Midweek!\\n");\n    \n    Data d;\n    d.i = 42;\n    printf("Int: %d\\n", d.i);\n    d.f = 3.14f;  // Overwrites same memory\n    printf("Float: %.2f\\n", d.f);\n    printf("Union size: %zu\\n", sizeof(Data)); // max member size\n    return 0;\n}`, quiz: { question: "What is unique about how a union stores data?", options: ["It stores multiple values simultaneously", "All members share the same memory location", "It's read-only", "It auto-selects the type"], correct: 1, explanation: "A union stores only one value at a time, with all members sharing the same memory location. Size equals the largest member." } },
  { id: 16, title: "File I/O", duration: "15 min", content: `<p>C provides file I/O through the <code>FILE</code> type and functions like <code>fopen</code>, <code>fclose</code>, <code>fprintf</code>, <code>fscanf</code>, <code>fread</code>, <code>fwrite</code>.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    // Write to file\n    FILE *f = fopen("data.txt", "w");\n    if (!f) { perror("fopen"); return 1; }\n    \n    fprintf(f, "Alien Code\\n");\n    fprintf(f, "Level: %d\\n", 99);\n    fclose(f);\n    \n    // Read back\n    f = fopen("data.txt", "r");\n    if (!f) { perror("fopen"); return 1; }\n    \n    char line[100];\n    while (fgets(line, sizeof(line), f)) {\n        printf("Read: %s", line);\n    }\n    fclose(f);\n    return 0;\n}`, quiz: { question: "Which function should always be called after fopen()?", options: ["fflush()", "fclose()", "rewind()", "fsync()"], correct: 1, explanation: "fclose() must always be called to flush buffers, release the file handle, and free resources after fopen()." } },
  { id: 17, title: "Preprocessor Directives", duration: "12 min", content: `<p>The C preprocessor runs before compilation, handling <code>#include</code>, <code>#define</code>, <code>#ifdef</code>, and macro expansion.</p>`, codeExample: `#include <stdio.h>\n\n// Constants\n#define MAX_SIZE 100\n#define PI 3.14159265f\n\n// Function macro\n#define MAX(a, b) ((a) > (b) ? (a) : (b))\n#define SQUARE(x) ((x) * (x))\n\n// Conditional compilation\n#ifdef DEBUG\n    #define LOG(fmt, ...) fprintf(stderr, "[DEBUG] " fmt "\\n", ##__VA_ARGS__)\n#else\n    #define LOG(fmt, ...)\n#endif\n\nint main() {\n    printf("Max size: %d\\n", MAX_SIZE);\n    printf("PI: %.5f\\n", PI);\n    printf("Max(3,7): %d\\n", MAX(3, 7));\n    printf("Square(5): %d\\n", SQUARE(5));\n    LOG("Value: %d", 42); // only in debug mode\n    return 0;\n}`, quiz: { question: "What does #define do in C?", options: ["Declares a variable", "Creates a text substitution macro", "Includes a header file", "Defines a function"], correct: 1, explanation: "#define creates a macro — the preprocessor replaces all occurrences of the name with the defined value/expression before compilation." } },
  { id: 18, title: "Header Files & Multi-file Programs", duration: "13 min", content: `<p>Large C programs split code across multiple files. Header files (.h) declare interfaces; source files (.c) implement them. Use include guards to prevent double inclusion.</p>`, codeExample: `// math_utils.h\n#ifndef MATH_UTILS_H\n#define MATH_UTILS_H\n\ntypedef struct { double x, y; } Point;\n\ndouble distance(Point a, Point b);\nint isPrime(int n);\n\n#endif\n\n// math_utils.c\n#include "math_utils.h"\n#include <math.h>\n\ndouble distance(Point a, Point b) {\n    return sqrt((a.x-b.x)*(a.x-b.x) + (a.y-b.y)*(a.y-b.y));\n}\n\nint isPrime(int n) {\n    if (n < 2) return 0;\n    for (int i = 2; i*i <= n; i++)\n        if (n % i == 0) return 0;\n    return 1;\n}\n\n// main.c\n#include <stdio.h>\n#include "math_utils.h"\n\nint main() {\n    Point a = {0,0}, b = {3,4};\n    printf("Distance: %.1f\\n", distance(a, b)); // 5.0\n    return 0;\n}`, quiz: { question: "What is the purpose of include guards (#ifndef/#define/#endif)?", options: ["Speed up compilation", "Prevent a header from being included multiple times", "Enable conditional features", "Define constants"], correct: 1, explanation: "Include guards prevent a header file from being included multiple times in the same compilation unit, avoiding duplicate declaration errors." } },
  { id: 19, title: "Recursion", duration: "13 min", content: `<p>Recursion is when a function calls itself. Every recursive function needs a <strong>base case</strong> (stops recursion) and a <strong>recursive case</strong>.</p>`, codeExample: `#include <stdio.h>\n\n// Fibonacci\nlong fib(int n) {\n    if (n <= 1) return n;          // base case\n    return fib(n-1) + fib(n-2);   // recursive case\n}\n\n// Tower of Hanoi\nvoid hanoi(int n, char from, char to, char aux) {\n    if (n == 1) {\n        printf("Move disk 1: %c -> %c\\n", from, to);\n        return;\n    }\n    hanoi(n-1, from, aux, to);\n    printf("Move disk %d: %c -> %c\\n", n, from, to);\n    hanoi(n-1, aux, to, from);\n}\n\nint main() {\n    for (int i = 0; i < 8; i++) printf("%ld ", fib(i));\n    printf("\\n");\n    printf("\\nHanoi (3 disks):\\n");\n    hanoi(3, 'A', 'C', 'B');\n    return 0;\n}`, quiz: { question: "What is the base case in recursion?", options: ["The first function call", "The condition that stops recursion", "The return value", "The recursive call"], correct: 1, explanation: "The base case is the condition that stops the recursion, preventing infinite loops and stack overflow." } },
  { id: 20, title: "Function Pointers", duration: "15 min", content: `<p>In C, functions have addresses in memory. Function pointers allow passing functions as arguments, implementing callbacks and strategies.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n\n// Function type declaration\ntypedef int (*CompareFunc)(const void*, const void*);\n\nint ascending(const void *a, const void *b) {\n    return *(int*)a - *(int*)b;\n}\nint descending(const void *a, const void *b) {\n    return *(int*)b - *(int*)a;\n}\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n}\n\nint main() {\n    int nums[] = {3, 1, 4, 1, 5, 9, 2, 6};\n    int n = sizeof(nums) / sizeof(nums[0]);\n    \n    qsort(nums, n, sizeof(int), ascending);\n    printArray(nums, n);\n    \n    qsort(nums, n, sizeof(int), descending);\n    printArray(nums, n);\n    return 0;\n}`, quiz: { question: "What is a function pointer used for?", options: ["Pointing to variables", "Storing and passing functions as values", "Creating anonymous functions", "Fast function calls"], correct: 1, explanation: "Function pointers store the address of a function, allowing it to be passed as an argument, stored in arrays, and called indirectly." } },
  { id: 21, title: "Linked Lists", duration: "18 min", content: `<p>A linked list is a dynamic data structure where each node contains data and a pointer to the next node. Unlike arrays, linked lists grow/shrink at runtime.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int data;\n    struct Node *next;\n} Node;\n\nNode* createNode(int data) {\n    Node *n = (Node*)malloc(sizeof(Node));\n    n->data = data;\n    n->next = NULL;\n    return n;\n}\n\nvoid prepend(Node **head, int data) {\n    Node *n = createNode(data);\n    n->next = *head;\n    *head = n;\n}\n\nvoid printList(Node *head) {\n    while (head) {\n        printf("%d -> ", head->data);\n        head = head->next;\n    }\n    printf("NULL\\n");\n}\n\nint main() {\n    Node *head = NULL;\n    for (int i = 5; i >= 1; i--) prepend(&head, i);\n    printList(head);\n    return 0;\n}`, quiz: { question: "What is the main advantage of a linked list over an array?", options: ["Faster random access", "Dynamic size — can grow/shrink at runtime", "Less memory usage", "Supports 2D data"], correct: 1, explanation: "Linked lists can grow and shrink dynamically. Arrays have fixed size (static arrays) or require reallocation (dynamic arrays)." } },
  { id: 22, title: "Stack & Queue (ADT)", duration: "16 min", content: `<p>Abstract Data Types (ADTs) define behavior, not implementation. Stack (LIFO) and Queue (FIFO) are fundamental ADTs implemented using arrays or linked lists.</p>`, codeExample: `#include <stdio.h>\n#define SIZE 100\n\n// Stack using array\ntypedef struct { int data[SIZE]; int top; } Stack;\nvoid initStack(Stack *s) { s->top = -1; }\nvoid push(Stack *s, int v) { s->data[++s->top] = v; }\nint pop(Stack *s) { return s->data[s->top--]; }\nint isEmpty(Stack *s) { return s->top == -1; }\n\nint main() {\n    Stack s;\n    initStack(&s);\n    push(&s, 10);\n    push(&s, 20);\n    push(&s, 30);\n    printf("Popped: %d\\n", pop(&s)); // 30 (LIFO)\n    printf("Popped: %d\\n", pop(&s)); // 20\n    return 0;\n}`, quiz: { question: "What does LIFO mean for a Stack?", options: ["Last In, Free Out", "Last Input, First Output", "Last In, First Out", "Linear Integer First Out"], correct: 2, explanation: "LIFO — Last In, First Out. The most recently added item is the first to be removed, like a stack of plates." } },
  { id: 23, title: "Sorting Algorithms", duration: "17 min", content: `<p>Sorting is fundamental to CS. Understanding different algorithms teaches complexity analysis.</p><h4>Common Algorithms:</h4><ul><li>Bubble Sort — O(n²) — simple</li><li>Selection Sort — O(n²) — fewer swaps</li><li>Insertion Sort — O(n²) — efficient for small/nearly sorted</li><li>Merge Sort — O(n log n) — stable, divide & conquer</li><li>Quick Sort — O(n log n) avg — most practical</li></ul>`, codeExample: `#include <stdio.h>\n\nvoid bubbleSort(int arr[], int n) {\n    for (int i = 0; i < n-1; i++) {\n        int swapped = 0;\n        for (int j = 0; j < n-i-1; j++) {\n            if (arr[j] > arr[j+1]) {\n                int t = arr[j]; arr[j] = arr[j+1]; arr[j+1] = t;\n                swapped = 1;\n            }\n        }\n        if (!swapped) break;  // Already sorted!\n    }\n}\n\nint main() {\n    int arr[] = {64, 25, 12, 22, 11};\n    int n = 5;\n    bubbleSort(arr, n);\n    for (int i = 0; i < n; i++) printf("%d ", arr[i]);\n    return 0;\n}`, quiz: { question: "What is the time complexity of Bubble Sort?", options: ["O(n)", "O(n log n)", "O(n²)", "O(log n)"], correct: 2, explanation: "Bubble Sort has O(n²) time complexity — for each of n elements, it potentially compares n elements, leading to n×n operations." } },
  { id: 24, title: "Binary Search", duration: "13 min", content: `<p>Binary search finds an element in a <strong>sorted</strong> array in O(log n) time by repeatedly halving the search space.</p>`, codeExample: `#include <stdio.h>\n\nint binarySearch(int arr[], int n, int target) {\n    int left = 0, right = n - 1;\n    \n    while (left <= right) {\n        int mid = left + (right - left) / 2;  // Prevents overflow\n        \n        if (arr[mid] == target) return mid;\n        if (arr[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n    return -1;  // Not found\n}\n\nint main() {\n    int arr[] = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int n = 10;\n    \n    int idx = binarySearch(arr, n, 23);\n    printf("Found 23 at index: %d\\n", idx); // 5\n    \n    idx = binarySearch(arr, n, 10);\n    printf("Search 10: %d\\n", idx); // -1\n    return 0;\n}`, quiz: { question: "What is the prerequisite for binary search?", options: ["The array must be small", "The array must be sorted", "The target must exist", "The array must use integers"], correct: 1, explanation: "Binary search requires a sorted array. It works by comparing the target to the middle element and eliminating half the search space each time." } },
  { id: 25, title: "Trees & Binary Search Trees", duration: "18 min", content: `<p>Trees are hierarchical data structures. A Binary Search Tree (BST) maintains the invariant: left child < parent < right child, enabling O(log n) search.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct BST_Node {\n    int value;\n    struct BST_Node *left, *right;\n} BSTNode;\n\nBSTNode* insert(BSTNode *root, int val) {\n    if (!root) {\n        BSTNode *n = malloc(sizeof(BSTNode));\n        n->value = val; n->left = n->right = NULL;\n        return n;\n    }\n    if (val < root->value) root->left = insert(root->left, val);\n    else if (val > root->value) root->right = insert(root->right, val);\n    return root;\n}\n\nvoid inorder(BSTNode *root) {\n    if (!root) return;\n    inorder(root->left);\n    printf("%d ", root->value);\n    inorder(root->right);\n}\n\nint main() {\n    BSTNode *root = NULL;\n    int vals[] = {5, 3, 7, 1, 4, 6, 8};\n    for (int i = 0; i < 7; i++) root = insert(root, vals[i]);\n    inorder(root);  // Prints sorted: 1 3 4 5 6 7 8\n    return 0;\n}`, quiz: { question: "What order does in-order traversal of a BST produce?", options: ["Random order", "Reverse sorted order", "Sorted ascending order", "Insertion order"], correct: 2, explanation: "In-order traversal (left-root-right) of a BST always produces elements in sorted ascending order." } },
  { id: 26, title: "Memory Management & Valgrind", duration: "15 min", content: `<p>Manual memory management is powerful but requires care. Common bugs: memory leaks, dangling pointers, double free, buffer overflows.</p><p>Valgrind is a tool for detecting memory errors.</p>`, codeExample: `#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// Safe string duplication\nchar* safeStrdup(const char *s) {\n    if (!s) return NULL;\n    char *copy = (char*)malloc(strlen(s) + 1);\n    if (!copy) return NULL;\n    strcpy(copy, s);\n    return copy;\n}\n\nvoid freeMemory(void **ptr) {\n    if (ptr && *ptr) {\n        free(*ptr);\n        *ptr = NULL;  // Prevent dangling pointer\n    }\n}\n\nint main() {\n    char *name = safeStrdup("Alien Coder");\n    printf("Name: %s\\n", name);\n    freeMemory((void**)&name);\n    // name is now NULL, safe to check\n    if (!name) printf("Memory freed!\\n");\n    \n    // Run with: valgrind --leak-check=full ./program\n    return 0;\n}`, quiz: { question: "What is a dangling pointer?", options: ["A pointer to a large array", "A pointer that points to freed/invalid memory", "A NULL pointer", "A pointer to a constant"], correct: 1, explanation: "A dangling pointer points to memory that has been freed or gone out of scope, causing undefined behavior when dereferenced." } },
  { id: 27, title: "Bitwise Operations & Flags", duration: "14 min", content: `<p>Bitwise operations manipulate individual bits. This is essential for embedded systems, network programming, and performance-critical code.</p>`, codeExample: `#include <stdio.h>\n\n// Permission flags using bits\n#define READ    (1 << 0)  // 0001 = 1\n#define WRITE   (1 << 1)  // 0010 = 2\n#define EXECUTE (1 << 2)  // 0100 = 4\n\nvoid printPerms(int perms) {\n    printf("R:%d W:%d X:%d\\n",\n           (perms & READ) != 0,\n           (perms & WRITE) != 0,\n           (perms & EXECUTE) != 0);\n}\n\nint main() {\n    int perms = READ | WRITE;  // 0011 = 3\n    printPerms(perms);\n    \n    perms |= EXECUTE;   // Set execute\n    perms &= ~WRITE;    // Clear write\n    printPerms(perms);\n    \n    // Check specific bit\n    printf("Can read: %s\\n", (perms & READ) ? "yes" : "no");\n    return 0;\n}`, quiz: { question: "How do you clear bit n in variable x?", options: ["x = x - (1 << n)", "x &= ~(1 << n)", "x ^= (1 << n)", "x |= (1 << n)"], correct: 1, explanation: "x &= ~(1 << n) creates a mask with bit n as 0 and ANDs it with x, clearing that bit while preserving others." } },
  { id: 28, title: "Signal Handling", duration: "13 min", content: `<p>Signals are asynchronous notifications sent to processes. C programs can register handlers for signals like SIGINT (Ctrl+C), SIGSEGV, SIGTERM.</p>`, codeExample: `#include <stdio.h>\n#include <signal.h>\n#include <stdlib.h>\n#include <unistd.h>\n\nvolatile sig_atomic_t running = 1;\n\nvoid handleSignal(int sig) {\n    if (sig == SIGINT) {\n        printf("\\nCaught SIGINT! Cleaning up...\\n");\n        running = 0;\n    }\n}\n\nint main() {\n    // Register signal handler\n    signal(SIGINT, handleSignal);\n    signal(SIGTERM, handleSignal);\n    \n    printf("Running... Press Ctrl+C to stop\\n");\n    \n    while (running) {\n        printf("Tick...\\n");\n        sleep(1);\n    }\n    \n    printf("Graceful shutdown complete.\\n");\n    return 0;\n}`, quiz: { question: "What signal is sent when user presses Ctrl+C?", options: ["SIGTERM", "SIGKILL", "SIGINT", "SIGHUP"], correct: 2, explanation: "SIGINT (Signal Interrupt) is sent to a process when the user presses Ctrl+C in the terminal." } },
  { id: 29, title: "Socket Programming", duration: "18 min", content: `<p>Sockets allow network communication. C's Berkeley Sockets API is the foundation of all network programming — TCP servers, HTTP, etc.</p>`, codeExample: `#include <stdio.h>\n#include <sys/socket.h>\n#include <netinet/in.h>\n#include <string.h>\n#include <unistd.h>\n\nint main() {\n    int serverFd = socket(AF_INET, SOCK_STREAM, 0);\n    \n    struct sockaddr_in addr;\n    addr.sin_family = AF_INET;\n    addr.sin_addr.s_addr = INADDR_ANY;\n    addr.sin_port = htons(8080);\n    \n    bind(serverFd, (struct sockaddr*)&addr, sizeof(addr));\n    listen(serverFd, 5);\n    printf("Server listening on port 8080...\\n");\n    \n    int clientFd = accept(serverFd, NULL, NULL);\n    char *response = "HTTP/1.1 200 OK\\r\\nContent-Length: 5\\r\\n\\r\\nHello";\n    send(clientFd, response, strlen(response), 0);\n    close(clientFd);\n    close(serverFd);\n    return 0;\n}`, quiz: { question: "Which function waits for and accepts an incoming connection?", options: ["listen()", "bind()", "connect()", "accept()"], correct: 3, explanation: "accept() blocks and waits for an incoming connection, returning a new socket file descriptor for the connected client." } },
  { id: 30, title: "Multithreading with pthreads", duration: "17 min", content: `<p>POSIX threads (pthreads) enable concurrent execution. Multiple threads run in the same process, sharing memory. Synchronization with mutexes prevents race conditions.</p>`, codeExample: `#include <stdio.h>\n#include <pthread.h>\n\npthread_mutex_t lock = PTHREAD_MUTEX_INITIALIZER;\nint counter = 0;\n\nvoid* increment(void *arg) {\n    int id = *(int*)arg;\n    for (int i = 0; i < 1000; i++) {\n        pthread_mutex_lock(&lock);\n        counter++;\n        pthread_mutex_unlock(&lock);\n    }\n    printf("Thread %d done\\n", id);\n    return NULL;\n}\n\nint main() {\n    pthread_t t1, t2;\n    int id1 = 1, id2 = 2;\n    \n    pthread_create(&t1, NULL, increment, &id1);\n    pthread_create(&t2, NULL, increment, &id2);\n    \n    pthread_join(t1, NULL);\n    pthread_join(t2, NULL);\n    \n    printf("Counter: %d\\n", counter); // Should be 2000\n    pthread_mutex_destroy(&lock);\n    return 0;\n}`, quiz: { question: "What is a mutex used for in multithreaded programming?", options: ["Creating threads", "Sharing memory faster", "Preventing race conditions by synchronizing access", "Killing threads"], correct: 2, explanation: "A mutex (mutual exclusion) ensures only one thread can access a critical section at a time, preventing race conditions and data corruption." } },
  { id: 31, title: "C Standard Library Deep Dive", duration: "14 min", content: `<p>The C standard library (libc) provides essential utilities. Beyond stdio.h, explore math.h, time.h, stdarg.h for variadic functions.</p>`, codeExample: `#include <stdio.h>\n#include <math.h>\n#include <time.h>\n#include <stdarg.h>\n\n// Variadic function\nint sum(int count, ...) {\n    va_list args;\n    va_start(args, count);\n    int total = 0;\n    for (int i = 0; i < count; i++)\n        total += va_arg(args, int);\n    va_end(args);\n    return total;\n}\n\nint main() {\n    // Math\n    printf("sqrt(2) = %.6f\\n", sqrt(2.0));\n    printf("pow(2,10) = %.0f\\n", pow(2.0, 10.0));\n    printf("ceil(2.3) = %.0f\\n", ceil(2.3));\n    \n    // Time\n    time_t t = time(NULL);\n    struct tm *lt = localtime(&t);\n    printf("Year: %d\\n", 1900 + lt->tm_year);\n    \n    // Variadic\n    printf("Sum: %d\\n", sum(4, 10, 20, 30, 40));\n    return 0;\n}`, quiz: { question: "What library provides sqrt() and pow() in C?", options: ["stdio.h", "stdlib.h", "math.h", "complex.h"], correct: 2, explanation: "math.h provides mathematical functions like sqrt(), pow(), sin(), cos(), ceil(), floor(), etc. Link with -lm flag." } },
  { id: 32, title: "Performance & Optimization", duration: "16 min", content: `<p>C's performance advantage comes from direct hardware control. Optimization techniques: compiler flags, cache-friendly code, SIMD, minimize heap allocations.</p>`, codeExample: `#include <stdio.h>\n#include <time.h>\n\n// Cache-friendly matrix traversal (row-major)\nvoid rowMajor(int mat[1000][1000], int n) {\n    long sum = 0;\n    for (int i = 0; i < n; i++)\n        for (int j = 0; j < n; j++)\n            sum += mat[i][j];  // Sequential memory access\n}\n\n// Register hint\nlong sumArray(int *arr, int n) {\n    register long sum = 0;\n    for (register int i = 0; i < n; i++)\n        sum += arr[i];\n    return sum;\n}\n\nint main() {\n    // Measure execution time\n    clock_t start = clock();\n    // ... work ...\n    clock_t end = clock();\n    double ms = (double)(end-start)/CLOCKS_PER_SEC*1000;\n    printf("Time: %.3f ms\\n", ms);\n    \n    // Compile with: gcc -O3 -march=native program.c\n    return 0;\n}`, quiz: { question: "Which compiler flag enables maximum optimization in GCC?", options: ["-O0", "-O1", "-O3", "-Omax"], correct: 2, explanation: "-O3 enables aggressive optimizations: loop unrolling, function inlining, vectorization. Use -O2 for safer optimization." } },
  { id: 33, title: "Makefile & Build Systems", duration: "13 min", content: `<p>Makefiles automate the compilation process. Make only recompiles changed files, saving time on large projects.</p>`, codeExample: `# Makefile\nCC = gcc\nCFLAGS = -Wall -Wextra -O2 -g\nLDFLAGS = -lm -lpthread\n\nTARGET = aliencode\nSRCS = main.c math_utils.c file_utils.c\nOBJS = $(SRCS:.c=.o)\n\n# Default target\nall: $(TARGET)\n\n$(TARGET): $(OBJS)\n\t$(CC) $(OBJS) -o $@ $(LDFLAGS)\n\t@echo "Build complete: $(TARGET)"\n\n%.o: %.c\n\t$(CC) $(CFLAGS) -c $< -o $@\n\nclean:\n\trm -f $(OBJS) $(TARGET)\n\n.PHONY: all clean`, quiz: { question: "What does 'make clean' typically do?", options: ["Compiles the project", "Formats the source code", "Removes compiled files and binaries", "Runs tests"], correct: 2, explanation: "make clean removes compiled object files and executables, forcing a fresh full compilation on the next build." } },
  { id: 34, title: "C99/C11/C17 Modern Features", duration: "14 min", content: `<p>Modern C standards added important features. C99: variable-length arrays, designated initializers, inline functions. C11: _Generic, _Static_assert, atomic types. C17: bug fixes.</p>`, codeExample: `#include <stdio.h>\n#include <stdbool.h>   // C99 bool\n#include <stdint.h>    // Fixed-width integers\n#include <assert.h>\n\n// C11 static assertion\n_Static_assert(sizeof(int) == 4, "int must be 4 bytes");\n\n// Fixed-width integers (portable)\ntypedef uint8_t  byte;\ntypedef uint32_t u32;\ntypedef int64_t  i64;\n\n// C11 _Generic (type-generic programming)\n#define TYPE_NAME(x) _Generic((x), \\\n    int: "int",         float: "float", \\\n    double: "double",   char*: "string", \\\n    default: "unknown")\n\nint main() {\n    bool valid = true;  // C99\n    u32 bigVal = 0xDEADBEEF;\n    i64 huge = 9223372036854775807LL;\n    \n    printf("Type: %s\\n", TYPE_NAME(3.14));\n    printf("Size: %zu\\n", sizeof(bigVal));\n    return 0;\n}`, quiz: { question: "What header provides bool type in C99?", options: ["stdbool.h", "types.h", "bool.h", "stdlib.h"], correct: 0, explanation: "stdbool.h (C99) provides the bool type and true/false constants, making boolean logic more readable in C." } },
  { id: 35, title: "Systems Programming Concepts", duration: "16 min", content: `<p>C is the language of systems programming. Process management, IPC (Inter-Process Communication), and kernel interfaces are all C's domain.</p>`, codeExample: `#include <stdio.h>\n#include <unistd.h>\n#include <sys/types.h>\n#include <sys/wait.h>\n\nint main() {\n    printf("Parent PID: %d\\n", getpid());\n    \n    pid_t pid = fork();\n    \n    if (pid < 0) {\n        perror("fork failed");\n        return 1;\n    } else if (pid == 0) {\n        // Child process\n        printf("Child PID: %d, Parent: %d\\n", getpid(), getppid());\n        // exec: replace with new program\n        // execl("/bin/ls", "ls", "-la", NULL);\n        return 0;\n    } else {\n        // Parent process\n        int status;\n        waitpid(pid, &status, 0);\n        printf("Child exited with status: %d\\n", WEXITSTATUS(status));\n    }\n    return 0;\n}`, quiz: { question: "What does fork() return in the parent process?", options: ["0", "-1 on failure only", "The child's process ID (PID)", "1"], correct: 2, explanation: "In the parent process, fork() returns the child's PID. In the child, it returns 0. On failure, it returns -1." } },
  { id: 36, title: "C Mastery — What's Next", duration: "18 min", content: `<p>You've mastered C — the language that powers the world's infrastructure. Operating systems, databases, compilers, embedded systems, and performance-critical software all rely on C.</p><h4>Your C Journey Ahead:</h4><ul><li><strong>C++</strong> — C with classes, templates, STL</li><li><strong>Embedded C</strong> — Arduino, STM32, bare-metal</li><li><strong>OS Development</strong> — write your own bootloader</li><li><strong>Compiler Design</strong> — write a compiler in C</li><li><strong>CUDA/OpenCL</strong> — GPU programming</li></ul><p>You now understand computing at its deepest level. Every high-level language you use builds on what C pioneered. Welcome to the elite tier of programmers.</p>`, codeExample: `// A tiny HTTP server in C — proof of mastery\n#include <stdio.h>\n#include <string.h>\n#include <sys/socket.h>\n#include <netinet/in.h>\n#include <unistd.h>\n\nint main() {\n    int server = socket(AF_INET, SOCK_STREAM, 0);\n    int opt = 1;\n    setsockopt(server, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));\n    \n    struct sockaddr_in addr = {\n        .sin_family = AF_INET,\n        .sin_port = htons(8080),\n        .sin_addr.s_addr = INADDR_ANY\n    };\n    \n    bind(server, (struct sockaddr*)&addr, sizeof(addr));\n    listen(server, 10);\n    printf("Alien HTTP Server on :8080\\n");\n    \n    while (1) {\n        int client = accept(server, NULL, NULL);\n        char buf[4096];\n        read(client, buf, sizeof(buf));\n        const char *res = "HTTP/1.1 200 OK\\r\\nContent-Length: 15\\r\\n\\r\\nAlien Code!\\n";\n        write(client, res, strlen(res));\n        close(client);\n    }\n}`, quiz: { question: "Which language is most directly descended from and builds upon C?", options: ["Java", "Python", "C++", "Go"], correct: 2, explanation: "C++ was created by Bjarne Stroustrup as 'C with Classes' — it's a direct superset of C with OOP, templates, and the STL." } }
];
